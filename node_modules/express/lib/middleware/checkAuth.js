module.exports = function checkAuth(options) {
	return function checkAuth(req, res, next) {
	    if(!req.session.email) {
	        res.send('You are not authorized to view this page.');
	    } else {
	        next();
	    }
	}
}