ul
  if(hasThreads)
    each val in threads
      li
        div
          a(href='/class/#{className}/#{val._id}')
            div.classtag ?
            div.classname
              div.posttitle #{val.title}
              div.postpreview #{val.content}
              div.posttime #{time[val._id]}
  else
    div#nothreads There doesn't seem to be any threads here.
div#new-thread.new-button  + New Thread
div#threads
  form(action='/addThread', method='post')
    label(for='thread-title') Title:&nbsp;
    input(type='text', name='thread[title]', id='thread-title' required)
    br
    textarea(name='thread[content]', id='thread-content' required)
    br
    input(type='hidden', name='thread[className]', value='#{className}')
    input(type='submit', value='Add Thread')
    br
    button#hide-thread Cancel
